import 'package:flutter/material.dart';

class PlantDoc extends StatelessWidget {
  const PlantDoc({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("plantDoc"),
    );
  }
}
