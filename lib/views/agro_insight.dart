import 'package:flutter/material.dart';

class AgroInsight extends StatelessWidget {
  const AgroInsight({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("agroInsight"),
    );
  }
}
